"""Shared TypedDict and dataclass definitions for cross-app use.

Placeholder module — populated only when a second consumer appears
(per the "build iteratively" rule).  Shipping this file now keeps later
imports stable without pre-defining structures that don't exist yet.
"""
